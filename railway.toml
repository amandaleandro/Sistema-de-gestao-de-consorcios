# Configuração Railway — monorepo
# No painel do Railway, crie 3 serviços:
#   1. PostgreSQL  → use o template do Railway
#   2. backend     → GitHub repo, Root Directory = backend
#   3. frontend    → GitHub repo, Root Directory = frontend
# Cada subpasta tem seu próprio railway.toml com as configs de build/deploy.
